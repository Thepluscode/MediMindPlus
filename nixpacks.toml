[phases.setup]
nixPkgs = ["nodejs_18", "python311", "gcc", "gnumake"]

[phases.install]
cmds = ["cd backend && npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["cd backend && npm run build"]

[start]
cmd = "cd backend && node dist/index.js"
