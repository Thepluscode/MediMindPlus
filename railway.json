{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm ci --legacy-peer-deps && (npx tsc -p tsconfig.railway.json || true)"
  },
  "deploy": {
    "startCommand": "cd /app/backend && (NODE_ENV=production npx knex --knexfile knexfile.js migrate:latest || true) && node dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60
  }
}
